import * as Actions from '../actions/SurveyListTypes';

const initialState = [];

const surveyList = (state = initialState, action) => { 
  switch (action.type) {

    default:
      return state;
  }
}

export default surveyList;